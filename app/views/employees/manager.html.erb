<style>
table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  text-align: center;
  padding: 8px;
}

tr:nth-child(even) {background-color: #f2f2f2;}
</style>
<center><h1><u>EMPLOYEES</u></h1></center>

<center>
<table>
    <tr>
        <th> ID </th>
        <th> EMPLOYEE ID </th>
        <th> NAME </th>
        <th> COMPANY </th>
        <th> DATE OF JOINING </th>
        <th> DEPARTMENT </th>
        <th> LEAVES </th>
        <th> ACTIVE </th>
        <th> MANAGER </th>
        <th> ACTIONS </th>
        <th> MANAGER'S DETAILS </th
    </tr>
    
    <tr>
            <td><%= @manager.id%></td>
            <td><%= @manager.emp_id%></td>
            <td><%= @manager.name%></td>
            <td><%= @manager.company%></td>
            <td><%= @manager.date_of_joining%></td>
            <td><%= @manager.department%></td>
            <td><%= @manager.leaves%></td>
            <td><% if @manager.is_active %> Yes <%else%>No<%end%></td>
            <td><% if @manager.is_manager %> Yes <%else%>No<%end%></td>
            <td>
              <%= link_to 'Edit',edit_employee_path(@manager)%>
              <br>
              <%= link_to 'Delete',delete_page_path(@manager), method: :delete%>
            </td>
            <td><% if @manager.manager_id %>  <%= link_to 'Show Manager Details',get_manager_path(@manager)%> <%else%>---<%end%></td>
        </tr>
   
</table>

<p> 
<%= link_to 'Managers details',new_employee_path%>
</center>